# Python
.venv/
.ruff_cache/
__pycache__/

# Data
*.csv
*.csv.xz
# `data/*`, not `data/`, so the station reference CSVs below can be re-included:
# a file can't be re-included while its parent directory is excluded.
data/*

# Small (~150K total) and needed by the analyze tests,
# so checked in rather than refetched from `data.ny.gov` by `mta-od-data prepare`.
!data/stations_complexes.csv
!data/stations_individual.csv

# Contains useful settings for JSON schemas.
!.vscode/
