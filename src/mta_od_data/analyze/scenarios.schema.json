{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "Deinterlining scenario file",
  "description": "A JSON array of deinterlining scenarios for `mta-od-data analyze deinterlining` (--scenario-file, or the scenarios.json catalog). See OverrideGroup/ScenarioFile in deinterlining.py for the models this is generated from.",
  "$defs": {
    "OverrideGroup": {
      "additionalProperties": false,
      "description": "One override group in a scenario file's `overrides` array: an\nadd/remove pair shared by every station in `stations` -- the usual\ncase, since a deinterlining change typically affects several stations\non one line the same way. `line` disambiguates a `stations` entry\nwhose bare name is shared by more than one real complex elsewhere in\nthe system (see `StationIndex.resolve`); leave it out when every name\ngiven here is already unique on its own.",
      "properties": {
        "line": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Line"
        },
        "add": {
          "items": {
            "type": "string"
          },
          "title": "Add",
          "type": "array"
        },
        "remove": {
          "items": {
            "type": "string"
          },
          "title": "Remove",
          "type": "array"
        },
        "stations": {
          "items": {
            "type": "string"
          },
          "minItems": 1,
          "title": "Stations",
          "type": "array"
        }
      },
      "required": [
        "stations"
      ],
      "title": "OverrideGroup",
      "type": "object"
    },
    "ScenarioFile": {
      "additionalProperties": false,
      "description": "One scenario in a scenario file's top-level JSON array (see\n`SCENARIO_FILE_ADAPTER`). `name` is the short, exact-match identifier\n`--scenario` selects by; `description` defaults to `name` when\nomitted. `category` groups related scenarios for `--category` -- omit\nit for a scenario that doesn't belong to one (as `\"Current\"` does).",
      "properties": {
        "name": {
          "minLength": 1,
          "title": "Name",
          "type": "string"
        },
        "description": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Description"
        },
        "category": {
          "anyOf": [
            {
              "type": "string"
            },
            {
              "type": "null"
            }
          ],
          "default": null,
          "title": "Category"
        },
        "overrides": {
          "items": {
            "$ref": "#/$defs/OverrideGroup"
          },
          "title": "Overrides",
          "type": "array"
        }
      },
      "required": [
        "name"
      ],
      "title": "ScenarioFile",
      "type": "object"
    }
  },
  "items": {
    "$ref": "#/$defs/ScenarioFile"
  },
  "type": "array"
}
