[project]
name = "mta-od-data"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]
